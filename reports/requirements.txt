# P4's deps. Install from repo root: pip install -r reports/requirements.txt
# (or fold these lines into the root requirements.txt / backend/requirements.txt)
weasyprint>=61
jinja2>=3.1
qrcode[pil]>=7.4
python-docx>=1.1
